import axios from 'axios';
import AuthHeader from './AuthHeader';

const API_URL = 'http://localhost8080/api/v1/';

// quiz/all?userId=
// export const getAllQuizzes = () {
  // how can i get the username from the auth header data? IF that's even what should be done

  // return axios.get(API_URL + 'quiz/all?userId=', { headers: authHeader() });
// };
